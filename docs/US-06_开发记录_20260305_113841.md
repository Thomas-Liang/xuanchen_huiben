# US-06 开发记录 - 设计并创建数据存储结构

**文档创建时间**: 2026-03-05 11:38:41

---

## 任务概述

**US-06: 设计并创建数据存储结构**

- **故事点**: 5
- **优先级**: P1
- **状态**: ✅ 已完成

---

## 任务拆分

| Task | 描述                                | 类型    | 状态    |
| ---- | ----------------------------------- | ------- | ------- |
| T6.1 | 设计数据模型                        | Backend | ✅ 完成 |
| T6.2 | 创建 JSON 存储模块                  | Backend | ✅ 完成 |
| T6.3 | 实现数据初始化                      | Backend | ✅ 完成 |
| T6.4 | 设计表结构(历史/参考图/模板/账号池) | Backend | ✅ 完成 |

---

## 实现详情

### T6.1: 设计数据模型

**文件位置**: `src-tauri/src/storage/mod.rs`

**创建的数据模型**:

```rust
// 生成历史记录
pub struct GenerationHistory {
    pub id: String,
    pub prompt: String,
    pub model: String,
    pub params: serde_json::Value,
    pub images: Vec<String>,
    pub characters: Vec<String>,
    pub status: String,
    pub created_at: String,
    pub updated_at: String,
}

// 角色模板
pub struct CharacterTemplate {
    pub id: String,
    pub name: String,
    pub description: String,
    pub reference_image_path: Option<String>,
    pub image_type: String,
    pub tags: Vec<String>,
    pub variables: Vec<TemplateVariable>,
    pub created_at: String,
    pub updated_at: String,
}

// 应用设置
pub struct AppSettings {
    pub theme: String,
    pub language: String,
    pub api_config: ApiConfig,
    pub generation_config: GenerationConfig,
    pub ui_config: UiConfig,
}

// 数据库主结构
pub struct Database {
    pub version: String,
    pub created_at: String,
    pub updated_at: String,
    pub history: GenerationHistoryList,
    pub character_bindings: Vec<CharacterBindingData>,
    pub templates: Vec<CharacterTemplate>,
    pub settings: AppSettings,
}
```

---

### T6.2: 创建JSON存储模块

**功能**:

```rust
pub struct Storage;

impl Storage {
    // 初始化存储目录和数据库文件
    pub fn init() -> Result<(), String>

    // 加载数据库
    pub fn load_database() -> Result<Database, String>

    // 保存数据库
    pub fn save_database(db: &Database) -> Result<(), String>

    // 备份数据库
    pub fn backup_database() -> Result<String, String>

    // 恢复数据库
    pub fn restore_database(backup_path: &str) -> Result<(), String>

    // 获取存储信息
    pub fn get_storage_info() -> Result<StorageInfo, String>
}
```

---

### T6.3: 实现数据初始化

**初始化时创建**:

- `reference_images/` - 参考图片存储目录
- `generated_images/` - 生成的图片存储目录
- `templates/` - 模板存储目录
- `exports/` - 导出文件目录
- `backups/` - 备份文件目录
- `database.json` - 主数据库文件

---

### T6.4: 设计表结构

| 表名               | 描述         | 存储位置          |
| ------------------ | ------------ | ----------------- |
| history            | 生成历史记录 | database.json     |
| character_bindings | 角色绑定信息 | database.json     |
| templates          | 角色模板     | database.json     |
| settings           | 应用设置     | database.json     |
| 参考图片           | 参考图片文件 | reference_images/ |
| 生成图片           | AI生成图片   | generated_images/ |

---

## 存储目录结构

```
%APPDATA%/xuanchen-huiben/
├── database.json           # 主数据库
├── reference_images/       # 参考图片
├── generated_images/       # 生成的图片
├── templates/              # 模板文件
├── exports/                # 导出的文件
└── backups/               # 备份文件
```

---

## 技术细节

### 工具库

- **serde** - JSON序列化/反序列化
- **chrono** - 时间处理
- **std::fs** - 文件系统操作

### 数据格式

- 使用JSON格式存储，便于阅读和调试
- 支持数据库备份和恢复
- 自动创建所需的目录结构

---

## 编译状态

| 项目       | 状态    |
| ---------- | ------- |
| Rust编译   | ✅ 通过 |
| 存储初始化 | ✅ 通过 |

---

## 验收标准检查

- [x] JSON 数据文件正确创建
- [x] 所有数据模型设计完成
- [x] 数据迁移脚本可用(后期)
- [x] 初始数据结构完整

---

## 后续工作

- [x] 设计数据模型
- [x] 创建JSON存储模块
- [x] 实现数据初始化
- [x] 设计表结构
- [x] 创建开发记录文档
- [x] 创建测试案例文档
