# US-009 批量生成并发数配置功能 - 开发记录

**User Story**: US-009
**标题**: 批量生成并发数可配置
**开发日期**: 2026-03-01
**状态**: 开发完成

---

## 功能概述

实现批量生成图片时可配置并发数（1-5），支持使用 seedream 或 banana 2 模型进行多并发同时生图。

---

## 完成的工作

### 1. 后端 Rust 实现

#### 1.1 修改 GenerationConfig 结构体

**文件**: `src-tauri/src/commands/image_generator.rs`

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GenerationConfig {
    pub model: String,
    pub width: u32,
    pub height: u32,
    pub count: u32,
    pub quality: String,
    pub size: Option<String>,
    #[serde(alias = "sequentialImageGeneration")]
    pub sequential_image_generation: Option<String>,
    #[serde(alias = "responseFormat")]
    pub response_format: Option<String>,
    pub watermark: Option<bool>,
    pub concurrency: Option<u32>,  // 新增字段
}
```

#### 1.2 修改 GenerationConfigBody

**文件**: `src-tauri/src/api.rs`

```rust
#[derive(Debug, Deserialize)]
pub struct GenerationConfigBody {
    // ... 其他字段
    concurrency: Option<u32>,  // 新增字段
}
```

#### 1.3 默认值设置

在 `get_default_generation_config` 函数中设置默认并发数为 2。

---

### 2. 前端实现

#### 2.1 类型定义

**文件**: `src/types/index.ts`

```typescript
export interface GenerationConfig {
  model: 'seedream' | 'banana_pro';
  width: number;
  height: number;
  count: number;
  quality: 'standard' | 'high' | 'ultra';
  size?: string;
  sequential_image_generation?: 'auto' | 'disabled';
  response_format?: 'url' | 'b64_json';
  watermark?: boolean;
  concurrency?: number; // 新增字段
}
```

#### 2.2 状态管理

**文件**: `src/App.tsx`

- 新增 `concurrency` 状态，默认值为 2
- 在 `loadGenerationConfig` 中加载已保存的并发配置
- 在 `handleSaveGenerationConfig` 中保存并发配置
- 在 `handleBatchGenerate` 中使用配置的并发数

#### 2.3 UI 实现

**文件**: `src/App.tsx`

在生成参数设置区域（Banana 2 配置下方）添加并发数输入框：

```tsx
<Col span={12}>
  <Text strong>并发数：</Text>
  <InputNumber
    min={1}
    max={5}
    value={concurrency}
    onChange={value => setConcurrency(value || 2)}
    style={{ width: '100%', marginTop: 4 }}
  />
</Col>
```

#### 2.4 批量拆分结果绑定角色图

**文件**: `src/App.tsx`

在批量拆分结果的展开面板中添加完整的角色绑定UI：

- 显示每个场景中的角色列表
- 显示角色头像（已绑定参考图则显示图片，否则显示首字母）
- 显示绑定状态标签（已绑定/未绑定）
- 提供"绑定参考图"和"解绑"按钮
- 绑定后生成图片时自动使用已绑定的参考图

#### 2.5 批量生成设置弹框

**文件**: `src/App.tsx`

在"全部生图"按钮旁边添加"设置"按钮，点击弹出设置弹框：

- 模型选择 (Seedream 4.5 / Banana 2)
- 并发数 (1-5)

**Seedream 参数**:

- 图片尺寸 (1K / 2K / 4K)
- 图片质量 (标准 / 高清 / 超清)
- 组图功能 (关闭 / 自动)
- 返回格式 (URL链接 / Base64)
- 水印 (有水印 / 无水印)

**Banana 2 参数**:

- 图片比例 (1:1 / 16:9 / 9:16 / 4:3 / 3:4)
- 分辨率 (1K / 2K / 4K)

---

## 技术细节

### 并发实现逻辑

使用 JavaScript 的 `Promise.all` 实现并发控制：

```typescript
const concurrencyLimit = concurrency;
const tasks = [...batchSplitResult.segments];

for (let i = 0; i < tasks.length; i += concurrencyLimit) {
  const chunk = tasks.slice(i, i + concurrencyLimit);
  await Promise.all(chunk.map((task, chunkIdx) => runTask(task, i + chunkIdx)));
}
```

### 配置持久化

并发数配置通过 `saveGenerationConfig` / `loadGenerationConfig` 与其他生成参数一起保存和加载。

#### 2.6 模式切换与结果显示

**文件**: `src/App.tsx`

- 批量/单图模式切换开关
- 批量模式下显示拆分结果区域
- 单图模式下隐藏拆分结果区域
- 切换模式时自动隐藏/显示对应结果

#### 2.7 参考图类型扩展

**文件**: `src/App.tsx`

参考图类型选项扩展为4种：

- 人物
- 人脸
- 全身
- 场景

#### 2.8 批量生成图片UI优化

**文件**: `src/App.tsx`

- 下载按钮显示在图片右上角
- 点击图片可放大预览

#### 2.9 Banana 2 API 多参考图修复

**文件**: `src-tauri/src/commands/image_generator.rs`

- 修复多参考图传递问题
- 修复MIME类型判断
- 添加参考图类型到提示词（人物/人脸/全身/场景）

---

## 测试要点

1. 验证并发数可设置为 1-5 之间的整数
2. 验证并发数配置可保存和加载
3. 验证不同并发数下批量生图的行为
4. 验证 seedream 和 banana 2 模型都支持并发配置
5. 验证模式切换时结果的显示/隐藏
6. 验证参考图类型选择（人物/人脸/全身/场景）
7. 验证批量生成图片的下载和预览功能
8. 验证人脸和全身参考图生成

---

## 文件修改清单

| 文件                                        | 修改类型 |
| ------------------------------------------- | -------- |
| `src-tauri/src/commands/image_generator.rs` | 修改     |
| `src-tauri/src/api.rs`                      | 修改     |
| `src/types/index.ts`                        | 修改     |
| `src/App.tsx`                               | 修改     |
