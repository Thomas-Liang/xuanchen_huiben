# AI绘画 - 开发记录

---

## Sprint 1: 基础UI搭建 (Week 1-2)

### 2026-03-04

#### US-01: 引入Tailwind CSS并配置主题系统

**完成的工作**:

1. **安装 Tailwind CSS 依赖**
   - 执行 `npm install -D tailwindcss postcss autoprefixer`
   - 安装 `@tailwindcss/postcss` (Tailwind v4 需要)

2. **配置 Tailwind**
   - 创建 `tailwind.config.js` 配置文件
   - 配置 PostCSS: `postcss.config.js`
   - 使用 Tailwind v4 新语法 `@import "tailwindcss"`

3. **创建全局样式**
   - 创建 `src/index.css` 包含:
     - 毛玻璃效果 `.glass-card`
     - 按钮样式 `.btn-primary`
     - 动画工具类
     - 骨架屏效果
   - 创建 `src/antd-compat.css` 处理 Ant Design 样式兼容

4. **配置主题变量**
   - CSS 变量定义亮色/暗色主题
   - 支持 `dark` class 切换

**新增/修改的文件**:

- `tailwind.config.js` - Tailwind 配置
- `postcss.config.js` - PostCSS 配置
- `src/index.css` - 全局样式
- `src/antd-compat.css` - Ant Design 兼容样式
- `src/main.tsx` - 引入 CSS 文件

---

## Sprint 0: 项目初始化

### 2026-02-25

#### 完成的工作

1. **Tauri + React 项目初始化**
   - 使用 `npm create tauri-app` 创建项目
   - 技术栈: React 18 + TypeScript + Vite + Tauri 2.x
   - 项目名称: xuanchen-huiben

2. **开发环境配置**
   - 安装 ESLint、Prettier
   - 安装 Zustand (状态管理)
   - 安装 Axios (HTTP请求)
   - 安装 Ant Design (UI组件)
   - 配置 `.eslintrc.json` 和 `.prettierrc`

3. **项目结构搭建**

   ```
   src/
   ├── components/     # React组件
   ├── hooks/          # 自定义Hooks
   ├── types/          # TypeScript类型定义
   ├── services/       # API服务
   ├── store/          # Zustand状态管理
   └── utils/          # 工具函数

   src-tauri/
   └── src/
       └── commands/   # Tauri命令
   ```

4. **核心类型定义** (`src/types/index.ts`)
   - `PromptSegment` - 提示词分段
   - `ParsedPrompt` - 解析结果
   - `CharacterRef` - 角色引用
   - `CharacterBinding` - 角色绑定
   - `ImageGenerationParams` - 出图参数
   - `APIConfig` - API配置

5. **状态管理** (`src/store/index.ts`)
   - 使用 Zustand + persist 中间件
   - 管理 API配置、解析结果、角色绑定、出图参数

6. **Rust后端命令** (`src-tauri/src/lib.rs`)
   - `parse_prompt` - 提示词解析（提取@角色、分段）
   - 支持中文字符识别

7. **前端界面** (`src/App.tsx`)
   - 提示词输入框
   - 解析结果显示（角色列表、分段结果）
   - Ant Design UI组件

8. **配置文件更新**
   - `tauri.conf.json` - 应用名称、大小、窗口配置
   - `package.json` - 项目名称、脚本配置
   - `Cargo.toml` - Rust依赖

#### 遇到的问题

1. **Rust编译链接问题**
   - MSVC链接器环境配置复杂
   - 当前环境: VS Build Tools 14.44 + Windows SDK 10.0.26100
   - 错误: `LINK : fatal error LNK1104: 无法打开文件"msvcrt.lib"`
   - 解决方案: 配置正确的环境变量后成功编译

---

### 2026-02-26

#### 完成的工作

1. **UI界面全新设计**
   - 重新设计 `src/App.tsx`，采用现代化卡片式布局
   - 渐变色顶部导航栏
   - 圆形角色头像
   - 分段标签高亮显示
   - 示例提示词快速点击
   - 优化交互体验和视觉效果

2. **Rust后端修复**
   - 修复中文字符边界处理问题 (byte index is not a char boundary)
   - 使用 `char_indices()` 正确处理UTF-8中文字符
   - 优化角色名提取逻辑

3. **构建脚本优化**
   - 创建 `run.ps1` 自动构建运行脚本
   - 配置正确的MSVC环境变量 (PATH, LIB)
   - 解决 `msvcrt.lib` 缺失问题

4. **代码提交**
   - 推送到GitHub: https://github.com/Thomas-Liang/xuanchen_huiben
   - 解决远程仓库合并冲突

#### 项目启动方式

```cmd
powershell.exe -ExecutionPolicy Bypass -File "C:\Users\Administrator\xuanchen_huiben\run.ps1"
```

---

## 待完成 (Sprint 1)

- [ ] US-001 提示词分割功能完善
- [ ] US-002 角色绑定功能
- [ ] US-003 AI出图功能
- [ ] US-004 参数配置
- [ ] US-005 桌面应用基础功能
- [ ] US-007 三方API配置

---

## 技术架构

```
┌─────────────────────────────────────────┐
│         Tauri Desktop App                │
│  ┌─────────────────┐  ┌──────────────┐  │
│  │ React Frontend  │◀▶│ Tauri Cmds  │  │
│  │ (TypeScript)    │  │   (Rust)     │  │
│  └─────────────────┘  └──────────────┘  │
└─────────────────────────────────────────┘
                   │
       ┌───────────▼───────────┐
       │   AI Model API        │
       │ (即梦 / Banana Pro)   │
       └───────────────────────┘
```

## 依赖版本

- React: 19.1.0
- TypeScript: 5.8.3
- Vite: 7.0.4
- Tauri: 2.x
- Zustand: 5.x
- Ant Design: 6.x
- Axios: 1.x
